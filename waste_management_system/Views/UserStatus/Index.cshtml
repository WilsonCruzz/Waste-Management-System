@model IEnumerable<waste_management_system.Models.UserStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeOfUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeOfUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserStatuId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserStatuId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserStatuId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
